rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // DEV ONLY — tighten before production.
    match /team/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
